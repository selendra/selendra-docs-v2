import React from "react";
import Card from "../components/card"
import getStarted from "../data/_getStarted.json"
import explore from "../data/_explore.json"
import network from "../data/_network.json"

export default function HelloWorld() {
  return (
    <div className=" px-0 md:px-[10%]">
      <div className="flex flex-col justify-center items-center   my-10">
      <div className="w-[90%] lg:w-[60%] text-center flex flex-col">
          <p className="text-xl md:text-2xl lg:text-4xl font-extrabold">
          Ecosystem
          </p>
          <p>
           Browse around and join our community as we push forward developing new
          and innovative blockchain applications designed that benefit us all.
          </p>
          </div>
      </div>

      <p className="text-lg md:text-xl lg:text-2xl font-bold">Network</p>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-4 mt-8">
        {network.map((res)=>(
          <Card key={res?.id} res={res} />
        ))}
      </div>
      <br />
      <br />
       <p className="text-lg md:text-xl lg:text-2xl font-bold"> Apps and DApps</p>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-4 mt-8">
        {getStarted.map((res)=>(
          <Card key={res?.id} res={res}/>
        ))}
      </div>

      <br />
      <br />

      <p className="text-lg md:text-xl lg:text-2xl font-bold">Explore our APIs</p>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">
        {explore.map((res)=>(
          <Card key={res?.id} res={res}/>
        ))}
      </div>

    </div>

); }
